#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged but do not block commits if it fails
# This keeps formatting/lint helpful, while ensuring you can still commit/push
npx lint-staged || {
	echo "[husky] lint-staged failed — continuing without blocking the commit.";
	exit 0;
}
